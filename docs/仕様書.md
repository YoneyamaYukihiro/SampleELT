# SampleELT 仕様書

## 概要

SampleELT は、Pentaho Spoon に類似したビジュアル ETL（Extract / Transform / Load）ツールです。
主要 RDBMS（**Oracle / MySQL / PostgreSQL / SQL Server / MariaDB / SQLite**）およびファイル（Excel / CSV / TXT / TSV）を対象に、データの抽出・変換・出力をドラッグ＆ドロップで設計・実行できます。
複数のパイプラインをまとめて順次実行する **ジョブ機能** と、定時・定期の **スケジュール実行機能** を備えます。

---

## 開発環境

| 項目 | 内容 |
|------|------|
| フレームワーク | .NET 10 / WPF |
| 開発環境 | Visual Studio 2022 以上 |
| 対象 OS | Windows 10 / 11 |

### NuGet パッケージ

| パッケージ | バージョン | 用途 |
|---|---|---|
| CommunityToolkit.Mvvm | 8.4.2 | MVVM パターン (RelayCommand / ObservableObject) |
| Oracle.ManagedDataAccess.Core | 23.26.100 | Oracle DB 接続 |
| MySqlConnector | 2.5.0 | MySQL / MariaDB DB 接続 |
| Npgsql | 9.0.3 | PostgreSQL DB 接続 |
| Microsoft.Data.SqlClient | 6.0.2 | SQL Server DB 接続 |
| Microsoft.Data.Sqlite | 9.0.0 | SQLite DB 接続（埋め込み） |
| EPPlus | 8.5.1 | Excel ファイル読み書き (非商用ライセンス) |

---

## プロジェクト構成

```
SampleELT/
├── SampleELT.csproj
├── App.xaml / App.xaml.cs           # 起動時に接続設定・スケジュール・ジョブ読み込み
│                                    # CLI モード: --run / --run-job / --convert-ktr
├── MainWindow.xaml / .cs            # メインウィンドウ (ダイアログ起動は StepDialogService に委譲)
│
├── Models/
│   ├── StepBase.cs                  # ステップ抽象基底クラス (NodeWidth/NodeHeight含む)
│   ├── StepType.cs                  # ステップ種別 Enum
│   ├── Pipeline.cs                  # パイプライン定義
│   ├── PipelineConnection.cs        # ステップ間接続定義
│   ├── DbConnectionInfo.cs          # DB接続情報 (DbType / DbEnvironment(DEV/STG/PRD) / IsReadOnly)
│   ├── ConnectionRegistry.cs        # 接続設定シングルトン (connections.json) — IConnectionStore 実装
│   ├── ConnectionSafety.cs          # 書き込みステップ判定 / 書き込み SQL 検出 / 接続詳細フォーマッタ / ConnectionResolutionException
│   ├── ScheduleEntry.cs             # スケジュール1件 (Target/Mode/Type/JobFilePath)
│   ├── ScheduleRegistry.cs          # スケジュール設定シングルトン (schedules.json) — IScheduleStore 実装
│   ├── Job.cs                       # ジョブ定義 (FilePath / Steps / LogMode)
│   ├── LogMode.cs                   # CLI 実行時のログファイル出力モード
│   ├── FieldMappingItem.cs          # SelectValues の 1 行分マッピング (Parse/Serialize 含む)
│   ├── Stores/                      # DI 軽量版インターフェイス (テストで差し替え可)
│   │   ├── IConnectionStore.cs      #   接続情報の参照系 (Default プロパティ付き)
│   │   └── IScheduleStore.cs        #   スケジュール永続化・実行時刻計算
│   └── Serialization/
│       └── PipelineSerializationModels.cs  # JSON シリアライズ用 DTO (旧 MainViewModel から分離)
│
├── Steps/
│   ├── DBInputStep.cs               # 全 DB 統合入力 — DbProvider.For() で動的選択し DbInputExecutor へ委譲
│   ├── DBOutputStep.cs              # 全 DB 統合出力 — 同上 (DbOutputExecutor)
│   ├── OracleInputStep.cs           # 旧 StepType=OracleInput 用シン・ラッパー (Provider 固定)
│   ├── OracleOutputStep.cs          #   後方互換のため StepType を保持しつつ Executor に委譲
│   ├── MySQLInputStep.cs            # 同上 (MySqlProvider)
│   ├── MySQLOutputStep.cs           # 同上
│   ├── PostgreSQLInputStep.cs       # 同上 (PostgreSqlProvider)
│   ├── PostgreSQLOutputStep.cs      # 同上
│   ├── SqlServerInputStep.cs        # 同上 (SqlServerProvider)
│   ├── SqlServerOutputStep.cs       # 同上
│   ├── SqliteInputStep.cs           # 同上 (SqliteProvider)
│   ├── SqliteOutputStep.cs          # 同上
│   ├── ExcelInputStep.cs            # Excel / CSV / TSV / TXT 入力
│   ├── ExcelOutputStep.cs           # ファイル出力 (Excel/CSV/TSV/TXT・日時トークン対応)
│   ├── FilterStep.cs                # 行フィルタ (フィールド間比較対応)
│   ├── CalculationStep.cs           # 計算フィールド追加 (dateDiffMinutes 含む)
│   ├── SelectValuesStep.cs          # フィールド選択・リネーム
│   ├── DBDeleteStep.cs              # キーによるDB行削除 (全 DB 対応)
│   ├── InsertUpdateStep.cs          # UPSERT — DbInsertUpdateExecutor に委譲
│   ├── DBUpdateStep.cs              # UPDATE のみ — DbUpdateExecutor に委譲
│   ├── ExecSQLStep.cs               # 任意SQL実行 (全 DB 対応)
│   ├── DummyStep.cs                 # パススルー (分岐終端)
│   ├── MergeJoinStep.cs             # 2ストリーム結合
│   └── SetVariableStep.cs           # 変数生成 (TODAY/YESTERDAY/相対日付)
│
├── Engine/
│   ├── ExecutionEngine.cs           # トポロジカルソート実行 (AllInputStreams セット + 事前安全検査)
│   ├── PipelineSafetyChecker.cs     # 未解決接続 / RO 書き込み / Production 書き込みを Block / Confirm に分類
│   ├── PipelineLoader.cs            # JSON → Pipeline オブジェクト復元
│   ├── JobExecutor.cs               # ジョブ内パイプラインを順次実行
│   ├── JobLoader.cs                 # Job の JSON ファイル読み書き
│   ├── DbProvider.cs                # DB 差分 (接続ファクトリ・プレースホルダ・UPSERT/UPDATE 構文・識別子クォート) を隠蔽する抽象 + 5 プロバイダ
│   ├── DbInputExecutor.cs           # SELECT 共通アルゴリズム (DbProvider 越しに発行)
│   ├── DbOutputExecutor.cs          # INSERT / UPSERT 共通アルゴリズム
│   ├── DbInsertUpdateExecutor.cs    # 明示キー UPSERT (Insert/Update ステップ用)
│   └── DbUpdateExecutor.cs          # キー一致 UPDATE のみ (DB Update ステップ用)
│
├── Services/
│   ├── TaskSchedulerHelper.cs       # schtasks.exe で Windowsタスクスケジューラ登録・削除 (Pipeline/Job 両対応)
│   ├── StepDialogService.cs         # 各ステップ設定ダイアログの起動を集約 (旧 MainWindow.Open*Dialog 16 個を統合)
│   └── PipelineSchedulerService.cs  # 1分周期タイマーでスケジュール評価・実行 (旧 MainViewModel.SchedulerTick から分離)
│
├── Tools/
│   ├── KtrToJsonConverter.cs        # KTR → SampleELT JSON への変換オーケストレータ (薄い)
│   ├── SqlColumnExtractor.cs        # SELECT 句から出力カラム名を構文解析で抽出 (?/:{name} 入り SQL でも可)
│   ├── OracleDataSourceParser.cs    # Oracle DataSource (EZ Connect / TNS Descriptor) を host/port/service に分解
│   └── Ktr/                         # KTR 変換のサブコンポーネント (Strategy パターン)
│       ├── KtrInternalTypes.cs      #   内部 DTO + KtrConvertContext + KtrConnectionResolver
│       ├── KtrSetVariablePatternDetector.cs  # RowGenerator+ScriptValueMod を SetVariable に圧縮
│       ├── KtrSqlPlaceholderNormalizer.cs    # `?` → `:{name}` 変換 (lookupStepName 引数でヒント生成)
│       ├── KtrPipelineSerializer.cs # JSON 出力
│       └── KtrStepConverters.cs     # IKtrStepConverter + 13 種の Strategy 実装 + 登録簿
│
├── ViewModels/
│   ├── MainViewModel.cs             # パイプライン管理・保存・読み込み (DTO/スケジューラは分離済み)
│   ├── StepNodeViewModel.cs         # ノード VM (NodeWidth/Height/ConnectionLabel)
│   ├── ConnectionViewModel.cs       # 接続線 VM
│   └── StepTypeHelper.cs
│
├── Controls/
│   ├── StepNodeControl.xaml         # ステップノード (リサイズグリップ付き)
│   └── SqlEditorBehavior.cs         # SQL 入力 TextBox 共通の Tab キー挙動 (Tab=スペース 4 個 / Ctrl+Tab=フォーカス移動)
│
├── Dialogs/
│   ├── ConnectionManagerDialog.xaml # DB接続設定管理 (Oracle/MySQL/PostgreSQL/SqlServer/MariaDB/Sqlite)
│   ├── DBInputDialog.xaml           # 全 DB 統合 Input (DbType フィルタオプションで Oracle/MySQL 専用化も可能)
│   ├── DBOutputDialog.xaml          # DB Output (コミットサイズ設定)
│   ├── DBDeleteDialog.xaml
│   ├── DBUpdateDialog.xaml          # UPDATE のみ
│   ├── InsertUpdateDialog.xaml      # UPSERT
│   ├── ExecSQLDialog.xaml
│   ├── ExcelInputDialog.xaml        # File Input
│   ├── ExcelOutputDialog.xaml       # File Output (日時トークン挿入パネル付き)
│   ├── FilterDialog.xaml
│   ├── CalculationDialog.xaml
│   ├── SelectValuesDialog.xaml
│   ├── DummyStepDialog.xaml
│   ├── MergeJoinDialog.xaml
│   ├── SetVariableDialog.xaml
│   ├── SQLEditorDialog.xaml         # SQL 拡大編集ダイアログ
│   ├── HelpDialog.xaml              # 使い方/仕様書 表示
│   ├── ScheduleManagerDialog.xaml   # スケジュール管理 (Pipeline/Job 切替・実行モード選択)
│   └── JobManagerDialog.xaml        # ジョブ管理 (新規/開く/保存/名前を付けて保存/手動実行)
│
├── Converters/
│   └── BoolToVisibilityConverter.cs
│
├── SampleELT.Tests/                 # xUnit ユニットテスト (DB ステップは SQLite で E2E 検証)
│   ├── Engine/
│   │   ├── DbProviderTests.cs
│   │   ├── DbExecutorSqliteTests.cs
│   │   ├── DbInsertUpdateExecutorSqliteTests.cs
│   │   └── DbUpdateExecutorSqliteTests.cs
│   ├── Models/Stores/
│   │   └── ConnectionStoreDefaultTests.cs
│   ├── Steps/                       # FilterStep / CalculationStep / SetVariableStep 等
│   └── Tools/
│       └── KtrToJsonConverterTests.cs
│
└── docs/
    ├── 仕様書.md
    └── 使い方.md
```

### リファクタリングの設計指針

- **DB プロバイダ抽象**: ADO.NET の差分（接続ファクトリ／プレースホルダ記法／UPSERT 構文／識別子クォート）を `DbProvider` 抽象クラスに集約し、各 DB 種別（Oracle/MySQL/PostgreSQL/SqlServer/Sqlite）は単一の Strategy として実装。新しい DB を追加する際は `DbProvider` を 1 つ実装するだけで済む。
- **DB ステップは委譲シェル**: `OracleInputStep` / `MySQLInputStep` 等は後方互換のため `StepType` 名を残しつつ、実体は `DbInputExecutor`／`DbOutputExecutor`／`DbInsertUpdateExecutor`／`DbUpdateExecutor` に委譲する 30 行未満のラッパー。
- **KTR 変換は Strategy + 登録簿**: `IKtrStepConverter` を実装した 13 種類のステップ変換器を `KtrStepConverterRegistry` で集中管理。新ステップ種別の追加は新クラス 1 ファイルのみで完結する。
- **DI 軽量版**: `Models/Stores/IConnectionStore.cs` などのインターフェイスに `Default` 静的プロパティを設け、テスト時はモックに差し替え可能。本番コードはデフォルトで Registry シングルトンを使う。
- **ダイアログサービス**: 16 種類のステップ設定ダイアログ起動ロジックを `Services/StepDialogService.cs` に集約。MainWindow から UI 起動責務を分離。

---

## アーキテクチャ

### データフロー（パイプライン）

```csharp
Task<List<Dictionary<string, object?>>> ExecuteAsync(
    List<Dictionary<string, object?>> inputData,
    IProgress<string> progress,
    CancellationToken ct)
```

- **inputData** : 前ステップ（単一）の出力行リスト
- **AllInputStreams** : 複数入力時（MergeJoin 用）— `AllInputStreams[0]` = Left、`[1]` = Right
- Output ステップは inputData をそのまま返す（後続ステップへのパススルー）

### 実行エンジン

`ExecutionEngine` : Kahn's Algorithm でトポロジカルソート → 依存順に非同期実行。  
`JobExecutor` : ジョブ内の `JobStep` を `Order` 順に並べ `ExecutionEngine` を順次呼び出す。`ContinueOnError = false` のステップでエラーが発生した場合はジョブを中断する。各パイプラインのログは `[i/N] パイプライン実行: ファイル名 « コメント »` の形式で出力される（コメント未設定時はファイル名のみ）。

### DB 接続設定

`ConnectionRegistry`（シングルトン）が全接続設定を保持し `connections.json` に永続化。各ステップは `Settings["ConnectionId"]` (Guid 文字列) で接続を参照する。

#### `DbConnectionInfo` の安全属性

| プロパティ | 既定値 | 役割 |
|---|---|---|
| `Environment` | `Development` | 環境タグ (`Development` / `Staging` / `Production`)。Production にすると `EnvironmentBadge="PRD"` / `EnvironmentColor="#D32F2F"` を返し、ノード枠と接続ラベルが赤色になる |
| `IsReadOnly` | `false` | true のときこの接続を使う書き込み系ステップ (`DBOutput` / `DBDelete` / `DBUpdate` / `InsertUpdate` / `ExecSQL`) は `PipelineSafetyChecker` で Block 判定される |
| `DisplayName` | (計算プロパティ) | `🔶 [PRD] Spirytus TRN01D 🔒` のように DB アイコン + 環境バッジ + 名前 + RO アイコンを連結。一覧 / コンボボックス / ノードラベルで統一表示 |

#### `ResolveConnectionString` の解決ルール (誤実行防止)

```
Settings["ConnectionId"] が指定されている?
  ├ Yes
  │   ├ Guid として解釈不能 → ConnectionResolutionException 投げる
  │   ├ Registry に存在しない → ConnectionResolutionException 投げる
  │   └ 解決成功 → その接続文字列を返す
  └ No
      └ Settings["ConnectionString"] (旧形式) があればそれを返す。なければ空文字
```

ポイントは **`ConnectionId` が指定されているのに解決できない場合、旧 `ConnectionString` キーへサイレントフォールバックしない**こと。フォールバックを許すと「接続を削除したらパイプラインが古い接続文字列で別 DB を叩く」という事故が起こり得たため。

#### `PipelineSafetyChecker.Check(pipeline)`

各ステップを走査し、以下を `Issue` として返す:

| 検査 | Severity |
|---|---|
| `ConnectionId` が Guid 不正 / Registry 未登録 | **Block** (実行を完全停止) |
| Read-only 接続 (`IsReadOnly=true`) への書き込み系ステップ | **Block** |
| Production 接続への書き込み系ステップ | **Confirm** (UI なら確認ダイアログ、Engine ヘッドレスではスルー) |
| ExecSQL の SQL が SELECT のみ (`ContainsWriteSql` 偽) | 確認/拒否いずれも対象外 |

呼び出し箇所:
1. `MainViewModel.RunPipeline` — UI で Block / Confirm を MessageBox で処理
2. `ExecutionEngine.ExecuteAsync` — Block のみ。検出時は `ConnectionResolutionException` を投げ、step 単体の catch を貫通する

#### 実行ログの接続詳細

ExecutionEngine は各ステップを実行する直前に、`Settings["ConnectionId"]` を持つステップについて `ConnectionSafety.DescribeConnection(conn)` を progress に出力する。出力例:

```
[受注書込] 接続: [PRD] Spirytus TRN01D (oracle://163.141.231.40:1521/MES02)
```

これにより、ログを見れば「どの DB に書いたか」が追跡可能になる。パスワードは含まれない。

#### Oracle DataSource パーサ

`Tools/OracleDataSourceParser.Parse(dataSource, out host, out port, out service)` は、Oracle 接続文字列の `Data Source` 値を 2 種類のフォーマットからホスト / ポート / サービス名に分解する。

| フォーマット | 例 | 解釈 |
|---|---|---|
| EZ Connect | `163.141.231.40:1521/MES02` | host=`163.141.231.40`, port=`1521`, service=`MES02` |
| TNS Descriptor | `(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=db01)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=PROD)))` | HOST / PORT / SERVICE_NAME を正規表現で抽出。SERVICE_NAME が無ければ SID も探す。引用符付き・大文字小文字混在も許容 |

`OracleConnectionStringBuilder` は保存時に EZ Connect を TNS Descriptor 形式へ正規化することがあるため、再ロード時には両方を解釈する必要がある。両形式の往復は `OracleDataSourceParserTests` の 7 ケースで検証している。

### 接続マネージャ (ConnectionManagerDialog) の編集モデル

`ConnectionManagerDialog` は明示的な保存ボタンを持たず、編集内容は次の各タイミングで自動的に `ConnectionRegistry.Save()` を呼んでファイル永続化する:

| タイミング | 実装 |
|---|---|
| 一覧の選択変更 | `ConnectionListBox_SelectionChanged` で `e.RemovedItems[0]` を `SaveEditsToModel` → `Save()`、その後 `LoadConnectionToForm` で新しい選択を表示 |
| DB 種別変更 | `DbTypeCombo_SelectionChanged` で `BuildConnectionStringFor(newType)` → `Save()` |
| 環境タグ変更 | `EnvironmentCombo_SelectionChanged` (Production への昇格は MessageBox 確認後にロールバック可能) → `Save()` |
| 🔒 読み取り専用変更 | `ReadOnlyCheck_Changed` → `Save()` |
| ＋ 追加 | `IsCurrentEntryUnedited()` で「名前が『新しい接続』のままサーバーも空」を検出して連打をブロック。それ以外は新規 `DbConnectionInfo` を追加して `Save()` |
| ↑↓ 移動 / 🗑 削除 | 即 `Save()` |
| ダイアログ閉じる | `OnClosing` で最終 `SaveEditsToModel` → `Save()` |

`SaveEditsToModel` 内では同名検出 (`WarnIfDuplicateName`) を行い、同じ名前の別接続が既に存在する場合は 1 度だけ警告を出す。

参照ステップ数は `FindReferencingSteps(conn)` がコンストラクタで受け取った `Pipeline?` の `Steps` を走査して算出する。MainWindow のツールバー経由で開いた場合は `_vm.CurrentPipeline` を渡し、ステップ設定ダイアログ経由で開いた場合は null (参照表示なし)。

#### サポート DB 一覧

| 種別 | アイコン | ドライバ | 既定ポート | UPSERT 構文 | 識別子引用 |
|---|---|---|---|---|---|
| Oracle | 🔶 | Oracle.ManagedDataAccess | 1521 | `MERGE INTO ... USING DUAL` | (なし) |
| MySQL | 🐬 | MySqlConnector | 3306 | `ON DUPLICATE KEY UPDATE` | `` `name` `` |
| PostgreSQL | 🐘 | Npgsql | 5432 | `ON CONFLICT DO UPDATE` | `"name"` |
| SQL Server | 🟦 | Microsoft.Data.SqlClient | 1433 | `MERGE` | `[name]` |
| MariaDB | 🦭 | MySqlConnector（共用） | 3306 | `ON DUPLICATE KEY UPDATE` | `` `name` `` |
| SQLite | 🪶 | Microsoft.Data.Sqlite | (ファイル) | `ON CONFLICT DO UPDATE` | `"name"` |

`DbConnectionInfo.DbType` enum に基づき、`DBInputStep` / `DBOutputStep` 等が実行時に各実装へ委譲する。MariaDB は MySQL と同一のドライバを共用するため、内部的には MySQL ステップにディスパッチされる。SQLite はサーバ・ユーザー・ポートを持たず、ファイルパスのみで接続する（接続文字列例: `Data Source=C:\path\to\file.db`）。

---

## ステップ仕様

### Input グループ

#### DB Input（統合 DB 入力）

| 設定項目 | 説明 |
|---|---|
| ConnectionId | 接続設定 ID（ConnectionRegistry から選択）|
| SQL | 実行する SELECT 文 |
| ExecuteEachRow | `true`：前ステップの各行を `?` パラメータとして渡し行ごとに実行 |

#### Excel Input（File Input）

| 設定項目 | 説明 |
|---|---|
| FilePath | 入力ファイルパス（.xlsx / .csv / .tsv / .txt） |
| Format | `Excel` / `CSV` / `TSV` / `TXT` |
| SheetName | 読み込むシート名（Excel のみ）|
| Delimiter | 区切り文字（CSV/TSV/TXT のみ）|
| Encoding | `UTF-8` / `UTF-8 BOM` / `Shift-JIS`（CSV/TSV/TXT のみ。デフォルト: UTF-8）|
| HasHeader | 1行目をヘッダーとして扱う（デフォルト: true）|
| MaxRows | 読み込む最大行数（省略時=無制限、プレビュー用途）|

`ExcelInputDialog` の「👁 プレビュー (先頭 50 行)」ボタンは `MaxRows="50"` で同じ `ExcelInputStep` を実行して結果を DataGrid に表示する。プレビュー専用パスを持たないため、本番実行と完全に同じパーサを通る。

---

### Output グループ

#### DB Output（統合 DB 出力）

| 設定項目 | 説明 |
|---|---|
| ConnectionId | 接続設定 ID |
| TableName | 出力先テーブル名 |
| Mode | INSERT / UPSERT |
| CommitSize | N 件ごとにコミット（デフォルト: 100、0 = 最後に一括コミット）|

#### File Output（ExcelOutput ステップ）

| 設定項目 | 説明 |
|---|---|
| FilePath | 出力ファイルパス（**日時トークン使用可**）|
| Format | `Excel` / `CSV` / `TSV` / `TXT` |
| SheetName | シート名（Excel のみ）|
| Delimiter | 区切り文字（CSV/TXT: `,`  TSV: `\t` ）|
| Encoding | `UTF-8` / `UTF-8 BOM` / `Shift-JIS` |
| IncludeHeader | ヘッダー行を出力する（デフォルト: true）|

**日時トークン**

ファイルパス内の `{フォーマット}` を実行時の `DateTime.Now` で展開する。C# 標準日時フォーマット文字列をすべて使用可能。

| トークン例 | 展開例 |
|---|---|
| `{yyyyMMdd}` | `20260403` |
| `{yyyy-MM-dd}` | `2026-04-03` |
| `{HHmmss}` | `143022` |
| `{yyyyMMdd_HHmmss}` | `20260403_143022` |
| `{yyyy}` / `{MM}` / `{dd}` | 年・月・日 単独 |

```
例: C:\output\report_{yyyyMMdd}.xlsx → C:\output\report_20260403.xlsx
```

出力先ディレクトリが存在しない場合は自動作成する。

---

### Transform グループ

#### Filter

| 設定項目 | 説明 |
|---|---|
| FieldName | フィルタ対象フィールド名 |
| Operator | `equals` / `notEquals` / `contains` / `greaterThan` / `greaterOrEqual` / `lessThan` / `lessOrEqual` / `isNull` / `isNotNull` |
| Value | 比較値（固定値）。`isNull` / `isNotNull` では使用しない |
| RightField | 比較対象フィールド名（指定時はフィールド間比較、Value より優先）|

`FilterDialog` の演算子コンボボックス選択時に、その下の `OperatorDescription` TextBlock に挙動の説明文を動的表示する（各 ComboBoxItem の ToolTip と同内容）。

#### Calculation

| 設定項目 | 説明 |
|---|---|
| OutputFieldName | 出力フィールド名 |
| ExpressionType | `add` / `subtract` / `multiply` / `divide`（0 除算は null）/ `concat` / `constant` / **`dateDiffMinutes`** |
| Field1 / Field2 | 演算対象フィールド名 |
| Constant | 固定値（constant 式の場合）|

`CalculationDialog` も同様に演算タイプ選択時に `ExpressionDescription` に説明文を動的表示する。

#### Select Values

| 設定項目 | 説明 |
|---|---|
| FieldMappings | 1 行 1 マッピング。シリアライズは `元名\|新名\|型\|残す` の `|` 区切り。旧フォーマット (`元名=新名` / フィールド名のみ) も読み込み可 |

**FieldMappingItem の各プロパティ**

| プロパティ | 説明 |
|---|---|
| SourceName | 入力フィールド名（定数行では空）|
| DestName | 出力フィールド名（空なら SourceName を継承。`EffectiveDestName` で取得）|
| DataType | 出力型: 空 / `string` / `int` / `long` / `decimal` / `double` / `datetime` / `bool`（空 = 変換なし、`InvariantCulture` でパース。失敗時は `null`）|
| IsIncluded | `false` の行は出力から除外される |
| ConstantValue | 定数行の固定値。`IsConstant = string.IsNullOrEmpty(SourceName) && !string.IsNullOrEmpty(ConstantValue)` |

**実行時の挙動**

- IsIncluded=false の行および SourceName が空で定数値も空の行は除外
- 入力が 0 行でも、`items.All(IsConstant)` であれば 1 行を出力（マスタ生成用途）
- マッピングが 1 件もなければ全行そのまま通過（パススルー）

**「↓ 前段から取得」**

`SelectValuesDialog.GetUpstreamFieldsAsync()` が直前ステップを 1 件だけ辿り、以下の戦略でフィールド名を収集する:

| 前段 StepType | 戦略 |
|---|---|
| `SetVariable` | `Settings["Fields"]` を改行区切りで解析し `変数名=式` の左辺だけ抽出 |
| `SelectValues` | `Settings["FieldMappings"]` を `FieldMappingItem.Parse` して `IsIncluded=true` の `EffectiveDestName` を返す |
| `DBInput` / `OracleInput` / `MySQLInput` / `PostgreSQLInput` / `SqlServerInput` / `SqliteInput` | `Settings["SQL"]` を `SqlColumnExtractor.Extract` で解析。プレースホルダ (`?` / `:{name}`) を含んでいても SELECT ... FROM 句から列名を抽出 |
| `ExcelInput` 等 | `MaxRows=5` 相当の軽量実行プレビューで先頭行のキーを抽出 |

`StepDialogService.ShowSettingsDialog(owner, stepVm, Pipeline? pipeline = null)` の 3 番目の引数で `Pipeline` を渡せる。null（既定）の場合は「↓ 前段から取得」ボタンが無効化される。`MainWindow` からは `_vm.CurrentPipeline` を常に渡す。

---

### DB Operation グループ

#### DB Delete

| 設定項目 | 説明 |
|---|---|
| ConnectionId | 接続設定 ID |
| TableName | 削除対象テーブル名 |
| KeyFields | キーフィールド名（カンマ区切り）|
| CommitSize | N 件ごとにコミット（デフォルト: 100）|

#### Insert / Update（UPSERT）

DB 種別に応じて以下の構文を自動選択する:

| DB | 構文 |
|---|---|
| Oracle / SQL Server | `MERGE` 文 |
| MySQL / MariaDB | `INSERT ... ON DUPLICATE KEY UPDATE` |
| PostgreSQL / SQLite | `INSERT ... ON CONFLICT(...) DO UPDATE SET ...` |

#### DB Update（UPDATE のみ）

INSERT しない。一致しない場合はスキップ。

#### Exec SQL

| 設定項目 | 説明 |
|---|---|
| ConnectionId | 接続設定 ID |
| SQL | 実行する SQL |
| ExecuteEachRow | `true`：前ステップの各行を ? パラメータとして渡し行ごとに実行 |

---

### Utility グループ

#### Merge Join

| 設定項目 | 説明 |
|---|---|
| JoinType | `INNER` / `LEFT OUTER` / `RIGHT OUTER` / `FULL OUTER` |
| KeyFields | 結合キーフィールド名（カンマ区切り）|

`MergeJoinDialog` も結合タイプ選択時に `JoinTypeDescription` に説明文を動的表示する。

#### Dummy / Set Variable

各ステップの詳細は「使い方.md」を参照。

---

## パイプライン保存形式

`.json` 拡張子で保存（JSONシリアライズ、PropertyNameCaseInsensitive 読み込み）。

```json
{
  "Name": "サンプルパイプライン",
  "Steps": [
    {
      "Id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
      "Name": "DB Input",
      "StepType": "DBInput",
      "CanvasX": 100, "CanvasY": 150,
      "NodeWidth": 150.0, "NodeHeight": 70.0,
      "Settings": {
        "ConnectionId": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
        "SQL": "SELECT * FROM orders",
        "ExecuteEachRow": "false"
      }
    }
  ],
  "Connections": [
    { "Id": "...", "SourceStepId": "...", "TargetStepId": "..." }
  ]
}
```

---

## ジョブ機能

### 概要

複数のパイプラインを指定した順序で順次実行する。ジョブ自体も `.job.json` ファイルとして保存・共有できる。

### Job モデル

| プロパティ | 説明 |
|---|---|
| Id | Guid（自動生成）|
| Name | ジョブ名 |
| Comment | ジョブ全体への補足メモ（任意・複数行可）|
| LogMode | `OnError`（既定）/ `Always` / `Never` — CLI 実行時のログファイル出力モード |
| Steps | JobStep のリスト（Order 昇順で実行）|
| FilePath | 保存先ファイルパス（JSON に含めない）|

### JobStep モデル

| プロパティ | 説明 |
|---|---|
| Order | 実行順序（0 始まり）|
| Name | コメント（任意）。`JobExecutor` の実行ログには `ファイル名 « Name »` の形式で併記される。空ならファイル名のみ表示 |
| PipelineFilePath | 実行するパイプラインの JSON ファイルパス |
| ContinueOnError | `true`：このステップでエラーが発生しても次のステップへ続行 |

### ジョブファイル形式

```json
{
  "Id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  "Name": "日次バッチ",
  "LogMode": "OnError",
  "Steps": [
    {
      "Id": "...",
      "Order": 0,
      "Name": "マスタ更新",
      "PipelineFilePath": "C:\\pipelines\\master_update.json",
      "ContinueOnError": false
    },
    {
      "Id": "...",
      "Order": 1,
      "Name": "集計処理",
      "PipelineFilePath": "C:\\pipelines\\aggregate.json",
      "ContinueOnError": true
    }
  ]
}
```

### ジョブのファイル管理

ジョブはレジストリを持たず、`.job.json` ファイル単体で管理される。`JobManagerDialog` で「📄 新規」「📂 開く」「💾 保存」「📋 名前を付けて保存」で操作する。「▶ 実行 / ■ 停止」で UI から手動実行も可能（ログはメインウィンドウのログパネルへ出力）。

---

## スケジュール機能

### ScheduleEntry モデル

| プロパティ | 説明 |
|---|---|
| Name | スケジュール名 |
| Comment | スケジュールへの補足メモ（任意・複数行可）|
| Target | `Pipeline`（パイプラインファイル直接実行）/ `Job`（ジョブファイル実行）。UI では参照ファイルの拡張子(`.job.json`)/内容(`Steps[0].PipelineFilePath`)から自動判定される |
| PipelineFilePath | Target = Pipeline の場合に使用 |
| JobFilePath | Target = Job の場合に使用 |
| Mode | `InApp`（アプリ起動中のみ）/ `TaskScheduler`（Windows タスクスケジューラ）|
| Type | `Daily` / `Weekly` / `Hourly` / `Interval` |
| IsEnabled | **InApp モード時** に `PipelineSchedulerService` が監視対象に含めるかどうか（既定: `false`）。TaskScheduler モードでは Windows タスク側の有効/無効が実際の実行を制御するため、このフラグは影響しない |

### スケジュール種別

| 種別 | 説明 | 設定項目 |
|---|---|---|
| Daily | 毎日 指定時刻 | HH:MM |
| Weekly | 毎週 指定曜日・時刻 | 曜日 + HH:MM |
| Hourly | 毎時 指定分 | MM |
| Interval | N 分ごと | 間隔（分）|

### 実行モード

| モード | 説明 |
|---|---|
| インアプリ | アプリが起動している間のみ `PipelineSchedulerService` 内の DispatcherTimer（1分周期）で監視・実行 |
| Windowsタスクスケジューラ | `schtasks.exe` でタスクを登録。アプリ未起動でも OS が定期実行。**Pipeline / Job どちらにも対応** |

スケジューラ Tick は `IScheduleStore.Default.CalcLastDueTime()` で直近の実行予定時刻を計算し、`LastRunTime` と比較して未実行であれば実行する。Target が Job の場合は `JobLoader.LoadFromFile()` → `JobExecutor.ExecuteAsync()` を呼び出す。

`LastRunTime` が `null` のエントリを初めて観測した tick では過去の予定時刻を catch-up せず、`LastRunTime = now` を記録して次の予定時刻まで待つ（スケジュール登録／有効化直後の即時実行を防ぐ）。

### Windows タスクスケジューラ登録時の挙動

`TaskSchedulerHelper.Register(entry)` が `entry.Target` で分岐し、登録するコマンドラインを切り替える。

| Target | 登録される `/TR` の中身 |
|---|---|
| Pipeline | `"<exe path>" --run "<entry.PipelineFilePath>"` |
| Job | `"<exe path>" --run-job "<entry.JobFilePath>"` |

タスクは `SampleELT\<サニタイズ済みスケジュール名>` という名前で登録される。タスク名で使えない文字 (`\ / : * ? " < > |`) は `_` に置換される。リネーム時は `entry.LastTaskName` に保持された旧タスクを自動削除する。

---

## 未保存変更の検出

### パイプライン

以下の操作で `IsModified = true` となりタイトルバーに `*` が付く。

- ステップの追加・削除・接続
- ステップ設定ダイアログを閉じた後

**保存時**（`SavePipeline`）、**新規作成**（`NewPipeline`）、**開く**（`LoadPipeline`）、**クリア**（`ClearCanvas`）の前に未保存確認ダイアログを表示。ウィンドウの `×` ボタンでも同様に確認する。

### ジョブ

以下の操作で `_isJobDirty = true` となりダイアログタイトルに `*` が付く。

- ジョブ名・コメント・ログ出力モードの変更
- ステップの追加・削除・移動（↑↓）・適用
- ステップ編集パネル下段（ファイル・コメント・エラー時続行）の編集

**ダイアログを閉じる時**に未保存確認を行う。ファイル保存（💾 保存 / 📋 名前を付けて保存）成功後にフラグをクリアする。手動実行（▶ 実行）時にも未保存なら「保存して実行 / 保存せず実行 / 中止」を確認する。

ステップ編集パネル下段の編集は `StepField_Changed` で dirty 扱いになるが、`StepsListView_SelectionChanged` での自動再読込時は `_suppressStepEvents` フラグで誤検知を防いでいる。

---

## UI コンポーネント

### ツールバー

| ボタン | 機能 |
|---|---|
| 📄 新規 | 新しいパイプライン（未保存確認あり）|
| 📂 開く | パイプライン JSON を開く（未保存確認あり）|
| 💾 保存 | パイプラインを保存 |
| ▶ 実行 | パイプラインを実行 |
| ■ 停止 | 実行をキャンセル |
| 🗑 クリア | キャンバスをクリア（未保存確認あり）|
| ⏰ スケジュール | スケジュール管理ダイアログを開く |
| 🔗 ジョブ | ジョブ管理ダイアログを開く |

### ステップノードカード

- **ステップ名**（太字）・**種別ラベル**・**接続設定名**（青字）を表示
- 右端の **青い丸** が出力ポート（ドラッグして接続）
- 右下の **リサイズグリップ** でノードサイズ変更可（最小 120×50px）

### 接続線の右クリック削除

接続線レイヤー（`ConnectionsControl`）は 2 重構造になっている:

| 役割 | 表示 |
|---|---|
| Hit-test 用透明線 | `Stroke="Transparent"` / `StrokeThickness="12"` / `Cursor="Hand"` / 右クリックで `ContextMenu「この接続を削除」` を表示 |
| 表示用接続線 | `Stroke="#2196F3"` / `StrokeThickness="2"` / `IsHitTestVisible="False"` |
| 矢印 Polygon | `IsHitTestVisible="False"` |

`DeleteConnection_Click` → `MainViewModel.DeleteConnection(ConnectionViewModel)` で `Connections` コレクションと `Pipeline.Connections` を同期削除し、`AddLog` でログを出し、`IsModified = true` を立てる。両端ノードの `NotifyConnectionChanged()` を呼んでノード側の表示も更新する。

### SQL 入力 TextBox の共通キー操作

`Controls/SqlEditorBehavior.HandlePreviewKeyDown(sender, e)` を `PreviewKeyDown` イベントにフックすることで、3 つの SQL 入力ダイアログ（`SQLEditorDialog` / `ExecSQLDialog` / `DBInputDialog`）で共通のキー挙動を提供する:

| キー | 動作 |
|---|---|
| `Tab`（単押し） | 選択範囲を半角スペース `TabSize = 4` 個に置換、キャレットを直後に移動 |
| `Ctrl+Tab` | WPF 既定挙動に委譲（次のコントロールへフォーカス移動） |
| `Shift+Tab` | WPF 既定挙動に委譲（タブ文字挿入）|

各 TextBox には `AcceptsTab="True"` を併設しているため、`Ctrl+Tab` を使わない限り通常のタブ移動には進まない。Tooltip にも「Tab でスペース 4 個挿入、Ctrl+Tab で次のコントロールへフォーカス移動」を明記している。

### DataGrid ヘッダーのアンダースコア表示

WPF の DataGrid 自動列ヘッダーは `_` をアクセスキー prefix（次の文字を下線付き表示）として消費する。`ExcelInputDialog` / `DBInputDialog` のプレビューグリッドでは `AutoGeneratingColumn` イベントで `e.Column.Header` の `_` を `__` にエスケープし、`sb_id` などのカラム名を意図通り表示する。

### 右パネル（スケジュール一覧）

登録済みスケジュールを一覧表示。各カードに **📄 パイプライン / 📦 ジョブ** のアイコンと **有効/無効トグルボタン** を持ち即座に切り替え・保存できる。  
`ScheduleManagerDialog` 内で **↑/↓ ボタン**による並び替えを行うと、ダイアログ閉じ時の `RefreshSchedulePanel()` で表示順がこの右パネルにも自動反映される。

ヘッダー右の **「有効のみ」** チェックボックス（既定 ON）でフィルタする:
- ON: `entry.IsEnabled == true` のエントリのみ表示
- OFF: 全件表示

### 接続・スケジュール一覧の並び替え

`ConnectionManagerDialog` / `ScheduleManagerDialog` の一覧右側に **↑/↓ ボタン** を配置。  
- 接続側は `ObservableCollection<DbConnectionInfo>.Move()` で並び替え、`ConnectionRegistry.Save()` で `connections.json` を即時更新
- スケジュール側は `List<ScheduleEntry>` のタプル swap で並び替え、`ScheduleRegistry.Save()` で `schedules.json` を即時更新

どちらも選択状態は維持される。

---

## バージョン管理

`SampleELT.csproj` の `<Version>` プロパティに SemVer 形式（例: `0.1.0`）でバージョンを記述する。  
ビルド時にアセンブリの `AssemblyVersion` / `FileVersion` / `InformationalVersion` へ反映され、`SampleELT.exe` のファイル詳細プロパティにも焼き込まれる。

| 場所 | 表示形式 | 取得元 |
|---|---|---|
| ヘルプ → バージョン情報 | `Version 0.1.0` | `App.AppVersion`（`Assembly.GetName().Version?.ToString(3)`）|
| exe ファイル詳細プロパティ | `0.1.0.0` | csproj の `<Version>` |

リリース時の手順:

1. `<Version>` を SemVer に従って手動更新（バグ修正 → patch、機能追加 → minor、互換性破壊 → major）
2. コミット → プッシュ → publish

任意で GitHub Releases と連携する場合は `git tag v0.1.0 && git push origin v0.1.0` を併せて行う。

### Publish プロファイル

`Properties/PublishProfiles/win-x64.pubxml` に Windows x64 用の発行プロファイルを定義している。Visual Studio の「発行」と CLI の両方で参照される:

```bash
dotnet publish SampleELT.csproj -p:PublishProfile=win-x64
```

| プロパティ | 値 |
|---|---|
| Configuration | Release |
| RuntimeIdentifier | win-x64 |
| SelfContained | **true** （.NET 10 Desktop Runtime を同梱）|
| PublishDir | `publish\win-x64\` |
| PublishTrimmed | false （WPF は Trim 非対応）|

self-contained のため、配布先 PC に .NET ランタイム未インストールでも `SampleELT.exe` が単独で起動する。ファイル一式のサイズは約 160MB。

---

## 永続化ファイル一覧

| ファイル | 内容 |
|---|---|
| `connections.json` | DB 接続設定一覧 |
| `schedules.json` | スケジュール設定一覧 |
| `*.json`（任意パス）| パイプライン定義 |
| `*.job.json`（任意パス）| ジョブ定義 |
