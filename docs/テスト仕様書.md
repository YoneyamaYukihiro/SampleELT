# SampleELT テスト仕様書

## 概要

`SampleELT.Tests` プロジェクトは xUnit ベースのユニットテスト群で、本番コード `SampleELT.csproj` の挙動を検証する。
合計 **176 件 / 19 クラス**。

| 観点 | 内容 |
|---|---|
| フレームワーク | xUnit 2.9.0 (`Microsoft.NET.Test.Sdk` 17.12.0) |
| ターゲット | `net10.0-windows7.0` (WPF 参照のため Windows 限定) |
| 主な依存 | `Microsoft.Data.Sqlite` (DB ステップの E2E 検証用)、`EPPlus` (Excel 出力検証用) |
| 並列実行 | xUnit 既定で並列。静的シングルトン (`IConnectionStore.Default`) を差し替えるテストは `[Collection("ConnectionStore")]` で直列化 |

## 実行方法

```bash
# 全件実行
dotnet test SampleELT.Tests/SampleELT.Tests.csproj

# クラス絞り込み
dotnet test SampleELT.Tests/SampleELT.Tests.csproj \
  --filter "FullyQualifiedName~PipelineSafetyCheckerTests"

# テスト名絞り込み
dotnet test SampleELT.Tests/SampleELT.Tests.csproj \
  --filter "FullyQualifiedName~Production"
```

## カバレッジ計測

```bash
# Coverlet (xUnit と同梱の coverlet.collector で動く) でカバレッジ取得
dotnet test SampleELT.Tests/SampleELT.Tests.csproj \
  --collect:"XPlat Code Coverage" \
  --results-directory ./TestResults

# ReportGenerator で HTML レポートを生成 (要 dotnet tool)
dotnet tool install -g dotnet-reportgenerator-globaltool   # 初回のみ
reportgenerator \
  -reports:"TestResults/**/coverage.cobertura.xml" \
  -targetdir:"TestResults/CoverageReport" \
  -reporttypes:Html
```

生成された `TestResults/CoverageReport/index.html` を開くと、行・ブランチ・メソッド別カバレッジが見られる。

> 💡 `coverlet.collector` パッケージは現状参照されていない。本格的にカバレッジを取りたい場合は `SampleELT.Tests.csproj` に `<PackageReference Include="coverlet.collector" Version="6.0.x" />` を追加すること。

---

## テスト一覧

### Engine / 接続安全

#### ConnectionResolutionTests (4 件)
`Models/ConnectionResolutionTests.cs` — `ResolveConnectionString` のサイレントフォールバック廃止を検証。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `NoConnectionId_FallsBackToLegacyConnectionString` | `ConnectionId` 未指定で旧 `Settings["ConnectionString"]` のみの場合、レガシー値を返す |
| 2 | `NoConnectionId_NoLegacy_ReturnsEmpty` | どちらも未指定なら空文字 |
| 3 | `UnresolvedConnectionId_Throws_NoLegacyFallback` | `ConnectionId` あり + Registry 未登録 → `ConnectionResolutionException` (旧 CS にフォールバックしない) |
| 4 | `MalformedConnectionId_Throws` | `ConnectionId` が Guid 形式でない → `ConnectionResolutionException` |

#### PipelineSafetyCheckerTests (10 件)
`Engine/PipelineSafetyCheckerTests.cs` — パイプライン実行前安全検査の各分岐を網羅。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `EmptyPipeline_NoIssues` | 空パイプラインは Issue 0 件 |
| 2 | `StepWithoutConnectionId_NotChecked` | Filter など `ConnectionId` 不要なステップは検査対象外 |
| 3 | `UnresolvedConnectionId_IsBlock` | 未登録 Guid → `Block` |
| 4 | `ReadOnlyConnection_OnWriteStep_IsBlock` | RO 接続 + DBDelete → `Block` |
| 5 | `ReadOnlyConnection_OnReadStep_NoIssue` | RO 接続 + DBInput → 問題なし (SELECT は許容) |
| 6 | `ProductionConnection_OnWriteStep_IsConfirm` | PRD 接続 + DBOutput → `Confirm` |
| 7 | `ProductionConnection_OnReadStep_NoIssue` | PRD 接続 + DBInput → 問題なし |
| 8 | `ExecSql_SelectOnly_OnProduction_NoIssue` | PRD + ExecSQL(SELECT) → 問題なし |
| 9 | `ExecSql_Update_OnProduction_IsConfirm` | PRD + ExecSQL(UPDATE) → `Confirm` |
| 10 | `ExecSql_DeleteOnReadOnly_IsBlock` | RO + ExecSQL(DELETE) → `Block` |

#### ExecutionEngineTests (5 件)
`Engine/ExecutionEngineTests.cs` — トポロジカルソート実行とキャンセル。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `EmptyPipeline_CompletesWithoutError` | ステップ 0 件でも例外なく完了 |
| 2 | `SingleStep_ReportsCompletion` | 1 ステップ実行で完了 progress を出す |
| 3 | `TwoStepsConnected_DataFlows` | A → B 連結で A の出力が B の入力に渡る |
| 4 | `SetVariable_OutputsOneRow_ReachedByNextStep` | SetVariable の 1 行出力が後段で受け取れる |
| 5 | `Cancellation_ThrowsOperationCanceled` | CT キャンセルで `OperationCanceledException` |

#### JobExecutorTests (7 件)
`Engine/JobExecutorTests.cs` — ジョブ (複数パイプライン順次実行)。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `EmptyJob_CompletesWithoutError` | ステップ 0 件のジョブも正常完了 |
| 2 | `SinglePipeline_ExecutesSuccessfully` | 1 パイプライン実行成功 |
| 3 | `MultiplePipelines_ExecutesInOrder` | `Order` 順に逐次実行される |
| 4 | `PipelineError_WithContinueOnError_Continues` | エラーかつ `ContinueOnError=true` で次へ進む |
| 5 | `PipelineError_WithoutContinueOnError_Throws` | エラーかつ `ContinueOnError=false` でジョブ中断 |
| 6 | `Cancellation_ThrowsOperationCanceled` | CT キャンセル |
| 7 | `ProgressReports_ContainJobName` | progress 出力にジョブ名が含まれる |

#### JobLoaderTests (8 件)
`Engine/JobLoaderTests.cs` — `.job.json` の保存・読み込みラウンドトリップ。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `SaveAndLoad_BasicJob_RoundTrips` | 基本 Job の保存→読み込みで同一 |
| 2 | `SaveToFile_SetsFilePath` | `SaveToFile` で `FilePath` プロパティが設定される |
| 3 | `LoadFromFile_SetsFilePath` | 読み込み時にも `FilePath` が設定される |
| 4 | `SaveAndLoad_JobWithSteps_RoundTrips` | Steps を持つジョブの完全ラウンドトリップ |
| 5 | `FilePathNotSerialized_NotInJson` | `FilePath` は JSON 本文に含まれない (JsonIgnore) |
| 6 | `LoadFromFile_InvalidFile_ThrowsException` | 不正 JSON → 例外 |
| 7 | `LoadFromFile_MissingFile_ThrowsException` | 存在しないファイル → 例外 |
| 8 | `SaveToFile_CreatesFile` | 新規ファイル作成 |

---

### Engine / DB Executor (SQLite で E2E 検証)

#### DbProviderTests (16 件)
`Engine/DbProviderTests.cs` — DB ごとのプレースホルダ / 識別子クォート / UPSERT 構文を `DbProvider` 抽象が正しくディスパッチするか。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `For_MapsKnownDbTypes` | 6 種の `DbType` → 各 Provider 実装に解決 |
| 2 | `Oracle_NamedPlaceholderHasFnPrefixAndColon` | Oracle 名前付き: `:fn` プレフィックス |
| 3 | `Oracle_PositionalPlaceholderUsesColon` | Oracle 位置パラメータ: `:p0` |
| 4 | `Oracle_PreprocessSqlStripsTrailingSemicolon` | 末尾セミコロン除去 |
| 5 | `MySql_UsesAtSignAndBackticks` | MySQL: `@param`, `` `name` `` |
| 6 | `MySql_PreprocessConnectionStringEnablesUserVariables` | MySQL CS に `AllowUserVariables=true` 付与 |
| 7 | `Postgres_UsesDoubleQuotes` | PostgreSQL: `"name"` |
| 8 | `SqlServer_UsesBrackets` | SQL Server: `[name]` |
| 9 | `Sqlite_UsesDoubleQuotes` | SQLite: `"name"` |
| 10 | `BuildInsertSql_DefaultUsesProviderQuoteAndPositional` | INSERT 構築: クォートと位置パラメータ |
| 11 | `BuildInsertSql_OracleHasNoQuoting` | Oracle はクォート無し |
| 12 | `BuildUpsertSql_MySqlUsesOnDuplicateKey` | MySQL UPSERT: `ON DUPLICATE KEY UPDATE` |
| 13 | `BuildUpsertSql_PostgresUsesOnConflict` | PostgreSQL UPSERT: `ON CONFLICT ... DO UPDATE` |
| 14 | `BuildUpsertSql_SqliteUsesOnConflict` | SQLite UPSERT: 同上 |
| 15 | `BuildUpsertSql_OracleUsesMerge` | Oracle UPSERT: `MERGE INTO ... USING DUAL` |
| 16 | `BuildUpsertSql_SqlServerUsesMerge` | SQL Server UPSERT: `MERGE` |

#### DbExecutorSqliteTests (10 件)
`Engine/DbExecutorSqliteTests.cs` — `DbInputExecutor` / `DbOutputExecutor` の E2E (実 SQLite を相手に)。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `Input_NoParams_ReturnsAllRows` | パラメータ無しの SELECT で全行取得 |
| 2 | `Input_NamedPlaceholder_BindsByFieldName` | `:{field}` 名前付きバインド |
| 3 | `Input_PositionalPlaceholder_BindsByOrder` | `?` 位置パラメータの順序バインド |
| 4 | `Input_ExecuteEachRow_LooksUpEveryRow` | `ExecuteEachRow=true` で前行ごとに再実行 |
| 5 | `Input_NullDbValueReturnsNull` | DB の NULL は C# `null` で返る |
| 6 | `Output_Insert_AppendsRows` | INSERT モードで行追加 |
| 7 | `Output_Upsert_UpdatesExistingRow` | UPSERT で既存キーは UPDATE |
| 8 | `Output_EmptyInput_NoOp` | 0 行入力で何もしない |
| 9 | `SqliteInputStep_DelegatesToExecutor` | `SqliteInputStep` シェルが Executor に委譲 |
| 10 | `SqliteOutputStep_DelegatesToExecutor` | `SqliteOutputStep` シェルも同上 |

#### DbInsertUpdateExecutorSqliteTests (6 件)
`Engine/DbInsertUpdateExecutorSqliteTests.cs` — `Insert/Update` (UPSERT) 専用 Executor。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `ExistingKey_UpdatesRow` | キー一致 → UPDATE |
| 2 | `MissingKey_InsertsRow` | キー無し → INSERT |
| 3 | `CompositeKey_UpdatesAndInserts` | 複合キーで部分一致 |
| 4 | `EmptyInput_NoOp` | 0 行入力 |
| 5 | `NoKeyFields_Throws` | KeyFields 未指定で例外 |
| 6 | `UpdateFields_FiltersInsertColumns` | UpdateFields 指定で INSERT 側のカラムも絞られる |

#### DbUpdateExecutorSqliteTests (7 件)
`Engine/DbUpdateExecutorSqliteTests.cs` — UPDATE のみ Executor (INSERT しない)。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `ExistingKey_UpdatesRow` | キー一致 → UPDATE |
| 2 | `MissingKey_DoesNotInsert` | キー無し → 何もしない (INSERT しない) |
| 3 | `CaseInsensitiveKeyLookup` | キーの大文字小文字を無視 |
| 4 | `UpdateFields_LimitsColumnsToUpdate` | UpdateFields で更新列を限定 |
| 5 | `NoKeyFields_Throws` | KeyFields 未指定で例外 |
| 6 | `EmptyInput_NoOp` | 0 行入力 |
| 7 | `PartialMatch_WarnsAboutMissingKeys` | 一部行のみキー一致で警告ログ |

---

### Steps

#### FilterStepTests (17 件)
`Steps/FilterStepTests.cs` — Filter ステップの全演算子。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `Equals_Match_ReturnsRow` | `equals` 一致で行通過 |
| 2 | `Equals_NoMatch_ReturnsEmpty` | `equals` 不一致で除外 |
| 3 | `Equals_CaseInsensitive_Match` | `equals` 大文字小文字無視 |
| 4 | `NotEquals_Match_ReturnsRow` | `notEquals` |
| 5 | `Contains_Match_ReturnsRow` | `contains` |
| 6 | `Contains_NoMatch_ReturnsEmpty` | `contains` 不一致 |
| 7 | `GreaterThan_Numeric_Match` | `greaterThan` 数値 |
| 8 | `GreaterThan_Numeric_NoMatch` | `greaterThan` 不一致 |
| 9 | `LessThan_Numeric_Match` | `lessThan` 数値 |
| 10 | `IsNull_NullValue_ReturnsRow` | `isNull` で NULL のみ通過 |
| 11 | `IsNull_NonNullValue_ReturnsEmpty` | `isNull` 非 NULL で除外 |
| 12 | `IsNotNull_NonNullValue_ReturnsRow` | `isNotNull` |
| 13 | `IsNotNull_NullValue_ReturnsEmpty` | `isNotNull` NULL で除外 |
| 14 | `RightField_FieldComparison_Match` | `RightField` でフィールド間比較 |
| 15 | `RightField_FieldComparison_NoMatch` | フィールド間比較不一致 |
| 16 | `MissingField_ReturnsEmpty` | 指定フィールドが存在しない行は除外 |
| 17 | `MultipleRows_FiltersCorrectly` | 複数行に対する正常動作 |

> 💡 `greaterOrEqual` / `lessOrEqual` は本番コードに実装済みだがテストが無い (後述「追加推奨」)。

#### CalculationStepTests (14 件)
`Steps/CalculationStepTests.cs` — 計算ステップの全演算タイプ。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `Add_TwoFields_ReturnsSum` | `add` 加算 |
| 2 | `Subtract_TwoFields_ReturnsDifference` | `subtract` 減算 |
| 3 | `Multiply_TwoFields_ReturnsProduct` | `multiply` 乗算 |
| 4 | `Divide_TwoFields_ReturnsQuotient` | `divide` 除算 |
| 5 | `Divide_ByZero_ReturnsNull` | 0 除算で `null` |
| 6 | `Concat_TwoFields_ReturnsConcatenated` | `concat` 文字列連結 |
| 7 | `Constant_NumericValue_ReturnsDouble` | `constant` 数値 |
| 8 | `Constant_StringValue_ReturnsString` | `constant` 文字列 |
| 9 | `Add_NullField_TreatedAsZero` | NULL は 0 扱い |
| 10 | `Add_NonNumericField_TreatedAsZero` | 非数値は 0 扱い |
| 11 | `OutputFieldName_CustomName_UsedCorrectly` | 出力フィールド名指定 |
| 12 | `PreservesOriginalFields` | 元のフィールドは保持 |
| 13 | `DateDiffMinutes_DateTimeValues_ReturnsMinutes` | `dateDiffMinutes` 分差計算 |
| 14 | `MultipleRows_AllProcessed` | 複数行を漏れなく処理 |

#### SetVariableStepTests (14 件)
`Steps/SetVariableStepTests.cs` — 日付変数生成。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `OutputsExactlyOneRow` | 出力は常に 1 行 |
| 2 | `Today_FormatsWithDateFormat` | `TODAY` の標準フォーマット |
| 3 | `Today_AlternativeDateFormat` | カスタムフォーマット |
| 4 | `Yesterday_IsOneDayBefore` | `YESTERDAY` |
| 5 | `TodayMinusN_SubtractsDays` | `TODAY-N` |
| 6 | `TodayPlusN_AddsDays` | `TODAY+N` |
| 7 | `MonthStart_IsFirstDayOfMonth` | `MONTH_START` |
| 8 | `MonthEnd_IsLastDayOfMonth` | `MONTH_END` |
| 9 | `YearStart_IsJanFirst` | `YEAR_START` |
| 10 | `YearEnd_IsDec31` | `YEAR_END` |
| 11 | `Literal_PassesThroughAsIs` | リテラル値そのまま |
| 12 | `MultipleFields_AllPresent` | 複数変数を全部出力 |
| 13 | `EmptyLines_AreSkipped` | 空行は無視 |
| 14 | `CaseInsensitive_Today` | `today` 等小文字でも認識 |

#### MergeJoinStepTests (5 件)
`Steps/MergeJoinStepTests.cs` — 2 ストリーム結合。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `InnerJoin_MatchedRowsOnly` | INNER 一致行のみ |
| 2 | `LeftOuterJoin_IncludesUnmatchedLeft` | LEFT OUTER で左側全行 |
| 3 | `FullOuterJoin_IncludesBothUnmatched` | FULL OUTER で両側未一致も |
| 4 | `InnerJoin_NoMatch_ReturnsEmpty` | INNER で一致無しは空 |
| 5 | `NoKeyFields_CrossJoin` | キー未指定でクロス結合 |

> 💡 RIGHT OUTER が未テスト (後述「追加推奨」)。

#### TableCompareStepTests (15 件)
`Steps/TableCompareStepTests.cs` — データ移行検証用の 2 ストリーム比較ステップ。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `Match_AllSame_NoOutput_WhenIncludeMatchedFalse` | MATCH 行は既定で抑制 |
| 2 | `Match_AllSame_IncludesRow_WhenIncludeMatchedTrue` | IncludeMatched=true で MATCH も出力 |
| 3 | `Diff_DetectsDifferingColumn` | 1 列だけ違う → DIFF 行と `_diff_columns` |
| 4 | `OnlyLeft_LeftHasNoMatchInRight` | 移行元のみ → ONLY_LEFT、`*_R` は null |
| 5 | `OnlyRight_RightHasNoMatchInLeft` | 移行先のみ → ONLY_RIGHT、`*_L` は null |
| 6 | `CompareFields_RestrictsColumnsInspected` | CompareFields 指定で比較対象列を限定 |
| 7 | `MultipleKey_HandlesCompositeKey` | 複合キー (dept,emp) で正しく突き合わせ |
| 8 | `NumericComparison_OneVsOnePointZero_AreEqual` | 数値型は `decimal` で比較し `1 == 1.0` |
| 9 | `NullValues_NullsEqualTrue_AreMatched` | 両側 NULL → 既定で MATCH |
| 10 | `NullValues_NullsEqualFalse_AreDifferent` | NullsEqual=false で両側 NULL も DIFF |
| 11 | `IgnoreCase_StringDifferOnlyByCase_IsMatch` | IgnoreCase で `alice == ALICE` |
| 12 | `TrimStrings_LeadingTrailingWhitespace_IsMatch` | TrimStrings で前後空白を吸収 |
| 13 | `NoKeyFields_Throws` | KeyFields 空で `InvalidOperationException` |
| 14 | `InferCompareFields_WhenCompareFieldsEmpty_UsesAllNonKeyColumns` | CompareFields 空 → 全列 - キー列を自動推定 |
| 15 | `Summary_Counts_AllFourStatuses` | 4 ステータスが全て出る複合シナリオ |

#### ExcelOutputStepTests (11 件)
`Steps/ExcelOutputStepTests.cs` — 日時トークン (`{yyyyMMdd}` 等) の解決ロジックのみテスト。実 Excel I/O は対象外。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `ResolveDateTokens_NoTokens_ReturnsUnchanged` | トークン無しは無変換 |
| 2 | `ResolveDateTokens_yyyyMMdd_ReplacesCorrectly` | `{yyyyMMdd}` 解決 |
| 3 | `ResolveDateTokens_DashDate_ReplacesCorrectly` | `{yyyy-MM-dd}` |
| 4 | `ResolveDateTokens_HHmmss_ReplacesCorrectly` | `{HHmmss}` |
| 5 | `ResolveDateTokens_MultipleTokens_AllReplaced` | 複数トークン全部置換 |
| 6 | `ResolveDateTokens_YearOnly_ReplacesCorrectly` | `{yyyy}` のみ |
| 7 | `ResolveDateTokens_MonthOnly_ReplacesCorrectly` | `{MM}` のみ |
| 8 | `ResolveDateTokens_DayOnly_ReplacesCorrectly` | `{dd}` のみ |
| 9 | `ResolveDateTokens_EmptyPath_ReturnsEmpty` | 空文字パス |
| 10 | `ResolveDateTokens_NoAtParam_UsesCurrentTime` | `DateTime.Now` フォールバック |
| 11 | `ResolveDateTokens_PathWithDirectory_PreservesDirectory` | ディレクトリ部分は維持 |

---

### Tools / Models

#### KtrToJsonConverterTests (3 件)
`Tools/KtrToJsonConverterTests.cs` — Pentaho KTR → SampleELT JSON 変換。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `Convert_BuildsPipelineWithExpectedSteps` | 標準的な KTR から期待ステップ群を生成 |
| 2 | `Convert_UnknownStepType_FallbackToDummyWithWarning` | 未知ステップは Dummy + 警告 |
| 3 | `Convert_ExecSql_BothExecSQLAndExecSQLRowMapToExecSQLStepType` | `ExecSQL` / `ExecSQLRow` 両方が `ExecSQL` ステップにマップ |

#### OracleDataSourceParserTests (7 件)
`Tools/OracleDataSourceParserTests.cs` — Oracle DataSource (EZ Connect / TNS Descriptor) 解析。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `EmptyString_ReturnsDefaults` | 空文字 → `localhost / 1521 / ORCL` |
| 2 | `EzConnect_HostPortService_Parsed` | `host:port/service` 分解 |
| 3 | `EzConnect_HostService_Only_PortDefaultsTo1521` | ポート省略 → 1521 |
| 4 | `TnsDescriptor_ServiceName_Parsed` | `(DESCRIPTION=...SERVICE_NAME=)` 分解 |
| 5 | `TnsDescriptor_SID_Fallback` | SERVICE_NAME 無 → SID にフォールバック |
| 6 | `TnsDescriptor_QuotedAndPadded_Parsed` | 引用符付き / 前後空白 |
| 7 | `TnsDescriptor_LowerCaseKeys_Parsed` | 小文字キー (description, host…) |

#### ScheduleRegistryTests (15 件)
`Models/ScheduleRegistryTests.cs` — `IScheduleStore.CalcLastDueTime` / `CalcNextRunTime` の各種日付ロジック。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `Daily_DueAlreadyPassedToday_ReturnsTodayDue` | 毎日: 今日の予定時刻を過ぎている |
| 2 | `Daily_DueNotYetToday_ReturnsYesterdayDue` | 毎日: 今日の予定時刻前 → 昨日の予定 |
| 3 | `Daily_ExactlyOnTime_ReturnsTodayDue` | 毎日: ちょうど時刻一致 |
| 4 | `Weekly_SameDayBeforeDue_ReturnsPreviousWeek` | 毎週: 同曜日かつ時刻前 → 前週 |
| 5 | `Weekly_SameDayAfterDue_ReturnsTodayDue` | 毎週: 同曜日かつ時刻後 → 今日 |
| 6 | `Weekly_DifferentDay_ReturnsLastOccurrence` | 毎週: 異なる曜日 → 直近の該当曜日 |
| 7 | `Hourly_MinutePassed_ReturnsCurrentHourDue` | 毎時: 当該分を過ぎた |
| 8 | `Hourly_MinuteNotYet_ReturnsPreviousHourDue` | 毎時: 当該分前 → 前時刻 |
| 9 | `Interval_NoLastRun_ReturnsNow` | インターバル: 初回 → 今 |
| 10 | `Interval_DuePassed_ReturnsDueTime` | インターバル: 経過済み |
| 11 | `Interval_DueNotYet_ReturnsNull` | インターバル: 未経過 → null |
| 12 | `NextRunTime_Daily_AfterDue_ReturnsTomorrow` | 次回実行 (毎日): 既に過ぎ → 翌日 |
| 13 | `NextRunTime_Daily_BeforeDue_ReturnsToday` | 次回実行 (毎日): 今日まだ → 今日 |
| 14 | `NextRunTime_Interval_WithLastRun_ReturnsLastRunPlusInterval` | 次回実行 (インターバル): 最終+間隔 |
| 15 | `NextRunTime_Interval_NoLastRun_ReturnsNow` | 次回実行 (インターバル): 初回 → 今 |

#### ConnectionStoreDefaultTests (2 件)
`Models/Stores/ConnectionStoreDefaultTests.cs` — `IConnectionStore.Default` 静的プロパティの差し替え。

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | `Default_DefaultsToConnectionRegistryInstance` | 既定は `ConnectionRegistry.Instance` |
| 2 | `Default_CanBeReplacedWithMock` | テストで自由に差し替え可能 |

---

## テストしていない領域

意図的に / 現状の制約で未テストの領域。実機検証や手動テストで補う。

### UI (WPF) 層
- **全 Dialog** (`ConnectionManagerDialog` / `DBInputDialog` / `ExcelInputDialog` / `SelectValuesDialog` 等) の振る舞い: フォーム編集・ボタン押下・コンボ選択など。WPF のテストには UI Automation または手動が必要
- `MainWindow` のドラッグ&ドロップ接続作成、ノードリサイズ、コンテキストメニュー
- `StepNodeControl` の DataTrigger 反映 (Production 赤枠 / 未解決バッジ)

### ViewModel 層
- `MainViewModel` の `RunPipeline` フロー全体 (`PipelineSafetyChecker` 連携は本番コードでは経路を作ったが MainViewModel 自体のテスト無し)
- `StepNodeViewModel.HasUnresolvedConnection` / `IsProductionConnection` の各ゲッター
- 接続線・接続削除のコマンド
- パイプライン保存・読み込み (`SaveToFile` / `LoadFromFile`)

### Services
- `TaskSchedulerHelper`: `schtasks.exe` の呼び出し (実 OS タスクスケジューラに依存するため難しい)
- `StepDialogService`: 16 種のダイアログ起動経路
- `PipelineSchedulerService`: 1 分タイマーの起動・停止

### Engine / 統合
- 本番 Oracle / MySQL / PostgreSQL / SQL Server / MariaDB は実 DB が必要なため未テスト。SQLite のみで構文の正しさを担保
- `JobExecutor` の各ステップで実 SQL 発行を伴う End-to-End シナリオ

### CLI
- `--run` / `--run-job` / `--convert-ktr` (`App.OnStartup` の引数処理)
- ログファイル生成 (`LogMode` のモード切替)

### Steps
- `ExcelInputStep` の実ファイル読み込み (Excel / CSV / TSV / TXT、各エンコーディング)
- `ExcelOutputStep` の実ファイル書き込み (シート上書き、複数シート、フォーマット切替)
- `SelectValuesStep`: 新フォーマット (`元名|新名|型|残す`) のラウンドトリップ、型変換、定数行、IsIncluded フィルタ
- `DBDeleteStep` / `DBOutputStep` / `InsertUpdateStep` の入力 0 行・エラー時挙動 (一部 Sqlite テストでカバー)
- `OracleInputStep` / `MySQLInputStep` 等の後方互換シェル

### Tools
- `SqlColumnExtractor`: `SELECT ... FROM` 句からのカラム名抽出ロジック (パイプライン: SelectValuesDialog の「↓ 前段から取得」が依存)
- `ConnectionSafety.ContainsWriteSql`: DML/DDL キーワード検出 (実コードはあるが PipelineSafetyChecker 経由でのみ間接的に検証)
- `ConnectionSafety.DescribeConnection`: 接続種別ごとのホスト/DB 名抽出

---

## 追加を推奨するテスト

優先度の高い順。事故の起こりやすさ・回帰の見逃しやすさで判断。

### 🔴 高優先度

| 提案 | 理由 |
|---|---|
| **`SelectValuesStep` の `FieldMappingItem.Parse` / `Serialize` ラウンドトリップ** | 新フォーマット `元名\|新名\|型\|残す` への移行で、旧フォーマット (`=` 区切り / 名前のみ) との後方互換が壊れると既存パイプラインで実行時に失敗する |
| **`SelectValuesStep.ConvertValue` の型変換** | int/long/decimal/double/datetime/bool それぞれの正常系 + パース失敗時 null フォールバック。データ精度バグの早期発見 |
| **`ConnectionSafety.ContainsWriteSql` の DML/DDL 判定** | `PipelineSafetyChecker` の安全機構の中核。コメント内キーワード、複数文、CTE 内 UPDATE などのエッジケース回帰防止 |
| **`SqlColumnExtractor.Extract` のパース** | SELECT 句に `DISTINCT` / `TOP N` / 関数 / alias / コメント / プレースホルダ込みのバリアント。Select Values の「↓ 前段から取得」が依存 |
| **`FilterStep` の `greaterOrEqual` / `lessOrEqual`** | 本番に実装済みだが現状テスト無し。仕様書では存在するため回帰を検知できない |

### 🟠 中優先度

| 提案 | 理由 |
|---|---|
| **`MergeJoinStep.RightOuterJoin`** | 4 種類のうち 1 種類のみテスト不足 |
| **`PipelineSafetyChecker` の追加ケース**: 同一接続を使う複数の書き込みステップ / 複数の Block と Confirm の混在 | 現実のパイプラインに近いシナリオ |
| **`ConnectionRegistry.Save`/`Load` のラウンドトリップ** (`Environment` / `IsReadOnly` 追加後の互換性) | 旧 connections.json から新フィールド既定値で読み込めるか |
| **`PipelineLoader` の不正 JSON / 欠損キー耐性** | エンドユーザーがファイルを手編集する可能性あり |

### 🟡 低優先度 (時間が取れるとき)

| 提案 | 理由 |
|---|---|
| **`ExcelInputStep` のフォーマット切替** (Excel / CSV / TSV / TXT × エンコーディング 3 種) | 実ファイルでの検証。サンプル `.xlsx` / `.csv` を `SampleELT.Tests/TestData/` に置く |
| **`KtrToJsonConverter` の追加ステップ種別** | 現在 3 件のみ。13 種ある Strategy のうち未網羅 |
| **`PipelineSchedulerService.SchedulerTick` の catch-up 抑制ロジック** | スケジュール登録/有効化直後の即時実行を防ぐ部分のテスト |
| **`OracleDataSourceParser` の不正入力** (途中で閉じカッコが欠落、複数 HOST/PORT、IPv6 アドレス) | パーサの堅牢性 |

---

## メンテナンス

- 新規テストを追加したら **本仕様書も更新** すること (`dotnet test` の合計件数も)
- テストクラスを新設する場合は本仕様書の「テスト一覧」に新セクションを追加
- 静的シングルトン (`IConnectionStore.Default` 等) を差し替えるテストは `[Collection("<名前>")]` を付け、並列実行で衝突しないようにする
