<Window x:Class="SampleELT.Dialogs.JavaScriptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JavaScript 設定"
        Width="560" Height="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ステップ名 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="ステップ名" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="StepNameBox" Height="28" Padding="6,4"/>
        </StackPanel>

        <!-- 実行モード -->
        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <CheckBox x:Name="RunPerRowCheck"
                      Content="入力の各行に対して実行（チェックなしの場合は1回のみ実行）"
                      IsChecked="True" FontSize="11"/>
        </StackPanel>

        <!-- JS コード -->
        <StackPanel Grid.Row="2" Margin="0,0,0,4">
            <TextBlock Text="JavaScript コード" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock FontSize="10" Foreground="#555555" TextWrapping="Wrap" Margin="0,0,0,4">
                入力フィールドは変数として参照可能。出力は $out オブジェクトに設定してください。&#10;例: $out.NEW_FIELD = FIELD_A + FIELD_B;
            </TextBlock>
        </StackPanel>
        <TextBox Grid.Row="3"
                 x:Name="ScriptBox"
                 Padding="6,4"
                 AcceptsReturn="True"
                 TextWrapping="Off"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 Margin="0,0,0,8"/>

        <!-- 注意書き -->
        <Border Grid.Row="4" Background="#E8F5E9" BorderBrush="#A5D6A7" BorderThickness="1"
                CornerRadius="4" Padding="8,6" Margin="0,0,0,12">
            <TextBlock FontSize="10" Foreground="#1B5E20" TextWrapping="Wrap">
                💡 使用例:&#10;
                var today = new Date();&#10;
                $out.result_date = today.getFullYear() + '/' + (today.getMonth()+1) + '/' + today.getDate();
            </TextBlock>
        </Border>

        <!-- ボタン -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Height="30" Margin="0,0,8,0"
                    Click="OK_Click" Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="キャンセル" Width="80" Height="30" Click="Cancel_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#EEEEEE"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="#CCCCCC"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4" Padding="12,6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E0E0E0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
