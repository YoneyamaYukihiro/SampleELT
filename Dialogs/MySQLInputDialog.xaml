<Window x:Class="SampleELT.Dialogs.MySQLInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MySQL Input 設定"
        Width="500" Height="530"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Step Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="ステップ名" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="StepNameBox" Height="28" Padding="6,4"/>
        </StackPanel>

        <!-- Server -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="サーバー" FontWeight="Bold" Margin="0,0,0,4"/>
                <TextBox x:Name="ServerBox" Height="28" Padding="6,4" Text="localhost"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="ポート" FontWeight="Bold" Margin="0,0,0,4"/>
                <TextBox x:Name="PortBox" Height="28" Padding="6,4" Text="3306"/>
            </StackPanel>
        </Grid>

        <!-- Database -->
        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="データベース" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="DatabaseBox" Height="28" Padding="6,4"/>
        </StackPanel>

        <!-- Username -->
        <StackPanel Grid.Row="3" Margin="0,0,0,8">
            <TextBlock Text="ユーザー名" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="UsernameBox" Height="28" Padding="6,4" Text="root"/>
        </StackPanel>

        <!-- Password -->
        <StackPanel Grid.Row="4" Margin="0,0,0,8">
            <TextBlock Text="パスワード" FontWeight="Bold" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordBox" Height="28" Padding="6,4"/>
        </StackPanel>

        <!-- SQL -->
        <StackPanel Grid.Row="5" Margin="0,0,0,4">
            <TextBlock Text="SQL クエリ" FontWeight="Bold" Margin="0,0,0,4"/>
        </StackPanel>
        <TextBox Grid.Row="6"
                 x:Name="SQLBox"
                 Height="80"
                 Padding="6,4"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 Margin="0,0,0,8"/>

        <!-- Test Connection -->
        <Button Grid.Row="7"
                Content="🔗 接続テスト"
                HorizontalAlignment="Left"
                Width="120"
                Height="30"
                Margin="0,0,0,10"
                Click="TestConnection_Click"
                Style="{StaticResource PrimaryButtonStyle}"/>

        <!-- OK / Cancel -->
        <StackPanel Grid.Row="8"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="80" Height="30"
                    Margin="0,0,8,0"
                    Click="OK_Click"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="キャンセル"
                    Width="80" Height="30"
                    Click="Cancel_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#EEEEEE"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="#CCCCCC"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4" Padding="12,6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E0E0E0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
