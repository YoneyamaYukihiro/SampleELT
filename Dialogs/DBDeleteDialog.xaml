<Window x:Class="SampleELT.Dialogs.DBDeleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DB Delete 設定"
        Width="500"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ステップ名 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="ステップ名" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="StepNameBox" Height="28" Padding="6,4"/>
        </StackPanel>

        <!-- 接続選択 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="DB 接続" FontWeight="Bold" Margin="0,0,0,4"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="110"/>
                </Grid.ColumnDefinitions>
                <ComboBox x:Name="ConnectionCombo"
                          Grid.Column="0" Height="28"
                          DisplayMemberPath="DisplayName"/>
                <Button Grid.Column="2"
                        Content="⚙ 接続設定管理"
                        Height="28"
                        FontSize="11"
                        Click="ManageConnections_Click"
                        Style="{StaticResource PrimaryButtonStyle}"/>
            </Grid>
            <TextBlock x:Name="NoConnectionHint"
                       Text="※ 接続が登録されていません。[接続設定管理] で追加してください。"
                       Foreground="#E53935" FontSize="11" Margin="0,4,0,0"
                       Visibility="Collapsed"/>
        </StackPanel>

        <!-- テーブル名 -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="テーブル名" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="TableNameBox" Height="28" Padding="6,4"/>
        </StackPanel>

        <!-- キーフィールド -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="キーフィールド（カンマ区切り）" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock Text="例: id  または  dept_id,emp_id" Foreground="#555555" FontSize="11" Margin="0,0,0,4"/>
            <TextBox x:Name="KeyFieldsBox" Height="28" Padding="6,4"/>
        </StackPanel>

        <!-- ボタン -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Content="🔗 接続テスト"
                    Width="110" Height="30"
                    Click="TestConnection_Click"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="OK"
                        Width="80" Height="30"
                        Margin="0,0,8,0"
                        Click="OK_Click"
                        Style="{StaticResource PrimaryButtonStyle}"/>
                <Button Content="キャンセル"
                        Width="80" Height="30"
                        Click="Cancel_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#EEEEEE"/>
                            <Setter Property="Foreground" Value="#333333"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="BorderBrush" Value="#CCCCCC"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4" Padding="12,6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#E0E0E0"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
