<Window x:Class="SampleELT.Dialogs.CalculationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calculation 設定"
        Width="420" Height="330"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Step Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="ステップ名" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="StepNameBox" Height="28" Padding="6,4"/>
        </StackPanel>

        <!-- Output Field Name -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="出力フィールド名" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="OutputFieldNameBox" Height="28" Padding="6,4" Text="Result"/>
        </StackPanel>

        <!-- Expression Type -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="演算タイプ" FontWeight="Bold" Margin="0,0,0,4"/>
            <ComboBox x:Name="ExpressionTypeCombo" Height="28"
                      SelectionChanged="ExpressionTypeCombo_SelectionChanged">
                <ComboBoxItem Content="add" IsSelected="True"/>
                <ComboBoxItem Content="subtract"/>
                <ComboBoxItem Content="multiply"/>
                <ComboBoxItem Content="divide"/>
                <ComboBoxItem Content="concat"/>
                <ComboBoxItem Content="constant"/>
            </ComboBox>
        </StackPanel>

        <!-- Field1 -->
        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="フィールド1" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="Field1Box" Height="28" Padding="6,4"/>
        </StackPanel>

        <!-- Field2 (hidden for constant) -->
        <StackPanel Grid.Row="4" x:Name="Field2Panel" Margin="0,0,0,10">
            <TextBlock Text="フィールド2" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="Field2Box" Height="28" Padding="6,4"/>
        </StackPanel>

        <!-- Constant (shown for constant type) -->
        <StackPanel Grid.Row="5" x:Name="ConstantPanel" Visibility="Collapsed" Margin="0,0,0,10">
            <TextBlock Text="定数値" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="ConstantBox" Height="28" Padding="6,4" Text="0"/>
        </StackPanel>

        <!-- OK / Cancel -->
        <StackPanel Grid.Row="7"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="80" Height="30"
                    Margin="0,0,8,0"
                    Click="OK_Click"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="キャンセル"
                    Width="80" Height="30"
                    Click="Cancel_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#EEEEEE"/>
                        <Setter Property="Foreground" Value="#333333"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="#CCCCCC"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4" Padding="12,6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E0E0E0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
